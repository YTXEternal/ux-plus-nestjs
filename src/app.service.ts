import { Injectable } from '@nestjs/common';
/**
 * 应用基础服务
 *
 * 该服务通常用于承载应用级别的通用业务能力或为 `AppController` 提供依赖。
 *
 * @export
 * @class AppService
 * @typedef {AppService}
 */
@Injectable()
export class AppService {
  /**
   * 构造函数
   */
  constructor() {}
}

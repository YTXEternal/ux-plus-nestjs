---
alwaysApply: false
description: 当用户输入"帮我生成API文档时"触发
---
# 核心规则
帮我生成一个API_DOCS.md文件（API文档）在根目录，接口包含：path（除了版本号和前缀）、请求参数（这个你根据DTO给我类型）、请求头、响应数据示例（如果data是数组你可以只展示第一元素，保证响应数据的完整性）、响应数据的类型（你要给出具体的TS类型）
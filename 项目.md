# 项目文档 (Project Documentation)

本文档旨在全面描述本项目的架构、技术栈、核心机制及业务模块功能，帮助开发者快速理解和上手。

## 1. 项目概览 (Project Overview)

本项目是基于 **NestJS (v11)** 框架开发的后端服务，采用模块化设计，集成了多种主流数据库和中间件，旨在提供高性能、可扩展的企业级后台管理解决方案。

### 1.1 技术栈 (Tech Stack)

*   **核心框架**: NestJS + TypeScript
*   **数据库**:
    *   **MySQL**: 使用 `Sequelize` (sequelize-typescript) 管理关系型数据（核心业务数据）。
    *   **MongoDB**: 使用 `Mongoose` 管理非结构化或高频数据（如聊天记录、日志）。
*   **缓存**: **Redis** (ioredis) 用于缓存、Session 管理、频率限制等。
*   **认证安全**:
    *   **JWT**: 双 Token 机制 (Access Token + Refresh Token)。
    *   **RSA**: 非对称加密，用于敏感信息传输（如登录密码）。
    *   **RBAC**: 基于角色的权限访问控制。
*   **实时通信**:
    *   **Socket.io**: 用于实时语音识别网关 (`speech`)。
    *   **SSE (Server-Sent Events)**: 用于 AI 聊天流式响应 (`chat`)。
*   **工具库**: `exceljs` (报表), `nodemailer` (邮件), `class-transformer`/`class-validator` (DTO 转换与验证)。

### 1.2 目录结构 (Directory Structure)

```
src/
├── databases/      # 数据库配置与模型 (MongoDB schemas, MySQL models)
├── dto/            # 通用数据传输对象 (Pagination, ApiResponse)
├── exceptions/     # 自定义异常类
├── filter/         # 全局异常过滤器 (HttpExceptionFilter)
├── guards/         # 守卫 (AuthToken, Permissions)
├── interceptors/   # 拦截器 (TransformResponse, XssSanitize, Timeout)
├── middleware/     # 中间件 (UniformResponseHeader)
├── modules/        # 通用功能模块 (Logger, Redis, Store, Permission, JWT, Password)
├── plugins/        # 应用插件配置 (Swagger, RateLimit, ValidationPipe)
├── routes/         # 核心业务路由模块 (Auth, System, Chat, Monitor, etc.)
├── services/       # 通用服务 (Email, Crypto)
├── tools/          # 工具函数库
└── main.ts         # 应用入口
```

---

## 2. 核心架构与机制 (Core Architecture)

### 2.1 认证与鉴权 (Auth & Authorization)

*   **双 Token 机制**:
    *   用户登录成功后返回 `accessToken` (短效) 和 `refreshToken` (长效)。
    *   `accessToken` 用于访问受保护接口，`refreshToken` 用于在 `accessToken` 过期后刷新获取新的 token。
*   **守卫 (Guards)**:
    *   `AuthTokenGuard`: 全局/路由级守卫。解析请求头 `Authorization` 中的 JWT，验证有效性，并将用户信息挂载到 `request.user`。支持 `@Public()` 装饰器跳过验证。
    *   `PermissionsGuard`: 权限守卫。配合 `@RequirePermissions('system:user:list')` 装饰器，检查用户是否拥有特定权限。超级管理员 (`*:*:*`) 拥有所有权限。
*   **加密传输**:
    *   登录密码使用 RSA 加密传输，后端使用私钥解密，确保传输安全。

### 2.2 请求处理流程 (Request Lifecycle)

1.  **Middleware**: `UniformResponseHeaderMiddleware` 设置统一的 HTTP 响应头。
2.  **Guards**: `AuthTokenGuard` -> `PermissionsGuard` 依次验证身份和权限。
3.  **Interceptors (Pre-Controller)**:
    *   `XssSanitizeInterceptor`: 对请求参数进行 XSS 过滤。
    *   `TimeoutInterceptor`: 设置接口超时时间。
4.  **Controller & Service**: 处理具体业务逻辑。
5.  **Interceptors (Post-Controller)**:
    *   `TransformResponseInterceptor`: 拦截响应数据。
        *   **统一格式**: 将返回值包装为 `{ code: 200, msg: '操作成功', data: ... }`。
        *   **DTO 转换**: 根据 `intercept.ts` 中的配置，基于 URL 和 Method 将响应数据转换为指定的 DTO 类（使用 `plainToClass`），自动剔除无关字段 (`excludeExtraneousValues`)。
6.  **Filters**:
    *   `HttpExceptionFilter`: 捕获所有异常，统一返回错误格式 `{ code: 500/40x, msg: '错误信息', data: null }`。

### 2.3 CPU 过载保护机制 (CPU Overload Protection)

项目内置了增强型动态请求丢弃算法，用于在服务器高负载时保护核心服务可用性。

*   **监控机制**:
    *   **定时采样**: 每 3 秒采集一次进程级 CPU 和内存使用率。
    *   **平滑算法**: 采用 **EMA (指数移动平均)** 算法处理 CPU 数据，避免因瞬间脉冲导致误判。
    *   **多维指标**: 同时监控 CPU（默认阈值：总核心数 * 80%）和内存（堆内存使用率 > 85%）。
*   **过载状态管理**:
    *   引入 `overloadTimes` 计数器（积分机制）。持续过载会增加计数，恢复正常会减少计数。这有效防止了保护机制在临界值附近频繁抖动（Flapping）。
*   **智能丢弃策略**:
    *   请求到达时，系统根据当前负载计算 **丢弃概率**。
    *   **概率公式**: $P = BaseProbability \times TimeFactor \times LoadFactor$
        *   `BaseProbability`: 基础丢弃率 (默认 0.7)。
        *   `TimeFactor`: 过载持续时间越长，权重越大（约 1 分钟达到最大值）。
        *   `LoadFactor`: 负载超出阈值越多，权重越大。
    *   **渐进式限流**: 在轻微过载初期仅丢弃少量非关键请求；随着负载持续升高，丢弃比例自动平滑上升至 100%，实现系统的自我保护与降级。

**核心机制总结**: 本系统通过定时采集并利用**EMA指数移动平均算法**平滑CPU与内存指标，结合**积分计数器**消除状态抖动，最终依据过载时长与负载强度的乘积动态计算请求**丢弃概率**，实现了从轻微限流到完全熔断的**渐进式自我保护**策略。

### 2.4 RBAC 权限控制系统 (RBAC Permission System)

项目实现了标准的 **RBAC (Role-Based Access Control)** 权限模型，支持精细到按钮级别的权限控制。

*   **模型设计**:
    *   **User (用户)**: 系统的操作主体。
    *   **Role (角色)**: 权限的集合载体，包含 `role_key` (如 `admin`)。用户与角色为多对多关系。
    *   **Menu (菜单/权限)**: 定义具体的资源操作，包含 `perms` 权限标识 (如 `system:user:add`)。角色与菜单为多对多关系。
*   **权限标识规范**:
    *   采用 `模块:功能:操作` 的字符串格式（例如 `system:user:list`, `file:upload:delete`）。
*   **鉴权流程**:
    1.  **定义权限**: 在 Controller 方法上使用装饰器 `@RequirePermissions('system:dept:add')` 标记所需权限。
    2.  **获取用户权限**: 请求进入 `PermissionsGuard` 后，调用 `SysPermissionService`。
        *   查询用户关联的所有角色。
        *   查询这些角色关联的所有菜单/按钮的 `perms` 字段，取并集。
    3.  **校验逻辑**:
        *   如果用户拥有 `SUPERADMIN` 角色，直接授予 `*:*:*` (所有权限)。
        *   否则，检查用户权限集合中是否包含接口要求的权限标识。
    4.  **结果**: 匹配成功则放行，否则抛出 403 Forbidden 异常。

**核心逻辑总结**: 本项目采用经典的 RBAC 模型，通过 `@RequirePermissions` 装饰器标记资源，`PermissionsGuard` 在运行时动态聚合用户角色关联的所有菜单权限标识并与资源要求比对，从而实现从菜单可见性到接口调用权的细粒度安全控制。

### 2.5 自动化测试与持续集成 (CI/CD)

项目配置了基于 **GitHub Actions** 的自动化测试流水线，确保代码质量和分支管理的规范性。

*   **触发机制 (Triggers)**:
    *   **Push**: 监听 `master`, `develop`, `release/*`, `hotfix/*`, `feature/*` 分支的代码提交。
    *   **Pull Request**: 监听针对 `master` 和 `develop` 分支的合并请求。
*   **分支保护策略 (Branch Rules)**:
    *   **Check-Branch-Rules Job**: 强制执行合并规范，**禁止**直接从普通分支合并到 `master`。只有 `hotfix/*` 或 `release/*` 分支的 PR 才允许合并入 `master`，确保生产环境的稳定性。
*   **测试环境 (Test Environment)**:
    *   运行于 `ubuntu-latest` 容器。
    *   **服务容器化**: 自动编排 **MySQL 9.3**, **Redis 8.0**, **MongoDB** 容器，为测试提供隔离且一致的数据库环境。
    *   **环境依赖**: Node.js v22, PNPM v10。
*   **测试流程 (Test Workflow)**:
    1.  **Integration Test**: 运行 `pnpm test` 执行单元与集成测试。
    2.  **E2E Test**: 运行 `pnpm test:e2e` 执行端到端测试。
    3.  **报告生成**: 测试完成后自动生成 JUnit 格式的测试报告 (`reports/report.xml`), 便于在 GitHub 上直观查看测试结果。

**核心流程总结**: 基于 GitHub Actions 构建了包含严格分支合并策略检查、全容器化数据库环境（MySQL/Redis/Mongo）及自动化集成/E2E 测试的 CI 流水线，全方位保障代码合规性与功能稳定性。

### 2.6 测试策略 (Testing Strategy)

项目采用 **Jest** 作为核心测试框架，结合 `@nestjs/testing` 和 `supertest` 实现了全面的自动化测试覆盖。

*   **单元测试 (Unit Testing)**:
    *   **文件规范**: 测试文件与源文件同级存放（或在 `tests` 子目录下），命名为 `*.spec.ts`。
    *   **测试范围**: 重点测试 Service 层的业务逻辑和工具函数（Tools）。
    *   **运行命令**: `pnpm test`
*   **端到端测试 (E2E Testing)**:
    *   **文件规范**: 存放于根目录 `test/` 下，命名为 `*.e2e-spec.ts`。
    *   **测试策略**:
        *   **真实环境**: 连接真实的测试数据库（MySQL/MongoDB），不使用 Mock 数据，确保业务链路的真实性。
        *   **全链路模拟**: 使用 `supertest` 模拟 HTTP 请求，覆盖从 Guard -> Interceptor -> Controller -> Service -> Database 的完整闭环。
        *   **独立性**: 每个测试套件（Suite）启动独立的 Nest 应用实例，并动态创建/销毁测试数据库，避免数据污染。
    *   **运行命令**: `pnpm test:e2e`

**核心测试逻辑总结**: 本项目基于 Jest 构建了双层测试体系，单元测试聚焦 Service 层逻辑验证，E2E 测试通过 Supertest 和真实数据库环境模拟完整 HTTP 请求链路，确保了从原子功能到系统集成的全方位质量保障。

---

## 3. 业务模块详解 (Business Modules)

业务逻辑主要位于 `src/routes` 目录下。

### 3.1 认证模块 (Auth)

*   **路径**: `/api/v1/auth`
*   **Controller**: `AuthController`
*   **核心功能**:
    *   `GET /public-key`: 获取 RSA 公钥，用于前端加密登录密码。
    *   `POST /login`: 用户登录。
        *   **请求**: `{ user_name, password }` (密码需经 RSA 加密)。
        *   **逻辑**: 验证凭据 -> 生成唯一 `tokenId` -> 存入 Redis (`login_tokens:${tokenId}`) 记录在线状态 -> 生成双 Token。
        *   **响应**: `{ token, refreshToken, apikey }`。
    *   `POST /refresh`: 刷新 Token。
        *   **请求**: `{ refreshToken }`。
        *   **逻辑**: 校验 Refresh Token 有效性 -> 签发新的 Access Token。
    *   `GET /info`: 获取当前用户信息及权限列表。
        *   **逻辑**: 根据 Token 解析 `user_id` -> 查询用户角色 -> 聚合所有角色的权限标识 (`permissions`)。
        *   **响应**: `{ user: {...}, roles: ['admin'], permissions: ['system:user:add', ...] }`。

### 3.2 系统管理模块 (System)

包含用户、角色、菜单、部门、字典等基础管理功能。

*   **菜单管理 (`/system/menu`)**:
    *   **核心逻辑**: 维护系统的功能菜单与权限标识。
    *   **数据结构**: 采用邻接表设计 (`menu_id`, `parent_id`) 存储树形结构。
    *   **接口详情**:
        *   `GET /list`: 获取菜单列表。支持按 `menu_name` 和 `status` 过滤。后端自动将扁平数据组装为 **树形结构** (`children` 嵌套) 返回。
        *   `POST /`: 新增菜单。需指定 `menu_type` (M:目录, C:菜单, F:按钮) 和 `perms` (权限标识)。
        *   `PUT /`: 修改菜单。
        *   `DELETE /`: 删除菜单。**约束**: 如果菜单包含子节点 (`children`), 则禁止删除，必须先删除子节点。
*   **用户管理 (`/system/user`)**:
    *   增删改查用户，分配部门和角色。
    *   `PUT /resetPwd`: 重置用户密码。
    *   `PUT /changeStatus`: 切换用户状态（正常/停用）。
*   **角色管理 (`/system/role`)**:
    *   定义角色及其对应的菜单权限。
    *   `GET /fulldata`: 获取所有角色列表（用于下拉选）。
*   **部门管理 (`/system/dept`)**:
    *   维护组织架构树。
*   **字典管理 (`/system/dict`)**:
    *   `Type`: 字典类型管理（如 `sys_user_sex`）。
    *   `Data`: 字典数据值管理（如 `男`, `女`）。

### 3.3 系统监控模块 (Monitor)

*   **登录日志 (`/monitor/logininfor`)**:
    *   记录所有登录尝试（成功/失败），包括 IP、地点、浏览器、操作系统等信息。
    *   提供清空和解锁用户功能。
*   **在线用户 (`/monitor/online`)**:
    *   从 Redis 查询当前活跃用户的 Token 信息。
    *   `DELETE /`: 强退在线用户。
*   **操作日志 (`/monitor/operlog`)**:
    *   记录用户的敏感操作（增删改），用于审计。

### 3.4 聊天模块 (Chat) - AI 集成

*   **技术**: MongoDB 存储, SSE 流式响应。
*   **功能**:
    *   `POST /session`: 创建新会话。
    *   `GET /sessions`: 获取历史会话列表。
    *   `GET /session/:sessionId/messages`: 获取会话消息记录。
    *   `POST /stream`: **AI 对话接口**。接收用户输入，调用 AI 服务，通过 SSE 实时推送回复内容。

### 3.5 文件管理模块 (File)

*   **功能**:
    *   `POST /upload`: 上传文件（支持 multipart）。
    *   `GET /list`: 文件列表查询。
    *   `DELETE /`: 删除文件。

### 3.6 动态路由 (Route)

*   **路径**: `/api/v1/route`
*   **Controller**: `RouteController`
*   **核心功能**:
    *   `GET /routers`: 获取路由表。
        *   **逻辑**: 根据 `user_id` 查询用户关联的菜单权限。后端自动过滤掉按钮级权限 (`menu_type=F`)，只返回目录和菜单，并递归构建为前端可渲染的路由树结构。
    *   `GET /getUserRoutes`: 获取用户路由名称。
        *   **响应**: `{ home: 'dashboard', routes: ['System', 'User', ...] }`。用于前端鉴权和菜单高亮。
    *   `GET /isRouteExist`: 校验路由名称唯一性。

**核心逻辑总结**: 系统采用 **JWT + RSA** 双重认证机制保障登录安全，通过 **RBAC** 模型将用户与角色、菜单权限解耦，后端提供 **树形结构化** 的菜单与路由数据接口，前端据此动态生成侧边栏与路由表，实现了前后端联动的权限控制体系。

### 3.7 语音网关 (Speech)

*   **技术**: WebSocket (Socket.io)。
*   **Gateway**: `SpeechGateway`
*   **功能**:
    *   `handleConnection`: 建立连接鉴权，初始化语音识别实例。
    *   `audio-stream`: 接收实时音频流，返回识别结果。

### 3.8 个人中心 (User Center)

*   **功能**:
    *   查询和修改当前登录用户的个人资料（头像、昵称、手机号等）。

---

## 4. 工具库 (Tools)

位于 `src/tools` 目录，提供通用辅助函数：

*   `parseAuthToken`: 解析 JWT Token 并获取用户信息（查 Redis/DB）。
*   `filterXss`: 过滤字符串中的 XSS 攻击脚本。
*   `md5`: MD5 加密工具。
*   `generateId`: 生成唯一 ID (Snowflake 或 UUID)。
*   `generateCode`: 生成随机验证码。
*   `transformEnv`: 环境变量类型转换。
*   `pagination`: 分页参数处理工具。

## 5. 环境配置与运行

确保本地环境已安装并运行以下服务：
*   **MySQL**: 默认端口 3306，需导入初始 SQL。
*   **Redis**: 默认端口 6379。
*   **MongoDB**: 默认端口 27017。

配置文件位于根目录 `.env` (开发环境) 或 `.env.production` (生产环境)。

---
*文档生成时间: 2026-03-17*

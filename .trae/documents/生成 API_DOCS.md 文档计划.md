我将执行以下步骤来生成 `API_DOCS.md` 文档：

1. **分析 API 结构**：

   * 基于已扫描到的控制器文件，梳理出所有功能模块（认证、系统管理、监控等）。

   * 整理每个模块下的 API 端点、HTTP 方法和路由路径。

2. **提取参数定义 (DTO)**：

   * 读取核心模块（如 `Auth`, `User`）的 DTO 文件，提取请求参数的具体字段、类型和必填项信息。

   * 对于其他标准 CRUD 模块（如角色、部门、字典等），根据通用模式推断参数结构（通常为分页查询参数、ID 参数或对应实体的创建/更新对象）。

3. **编写 API\_DOCS.md**：

   * 在项目根目录创建或覆盖 `API_DOCS.md` 文件。

   * 文档结构包含：

     * **项目概览**：基础 URL 前缀、通用响应格式 (`ApiResponse`)。

     * **模块详情**：按功能模块（Auth, System, Monitor）分类。

     * **接口定义**：每个接口包含 接口名称、URL、请求方式、请求参数（Query/Body/Param）、响应示例说明。

4. **验证与完善**：

   * 确保文档中的路由路径与代码实际路径一致（考虑全局前缀和版本号）。

   * 确保涵盖所有已扫描到的控制器。

**主要涉及的模块**：

* **认证模块**: 登录

* **系统管理**: 用户、角色、菜单、部门、岗位、字典、参数配置、通知公告

* **监控模块**: 操作日志、登录日志、在线用户


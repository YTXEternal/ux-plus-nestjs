# 添加 del_flag 字段到字典表

## 1. 数据库结构变更
执行 SQL 语句，为 `sys_dict_type` 和 `sys_dict_data` 表添加 `del_flag` 字段。
- **表**: `sys_dict_type`
  - **字段**: `del_flag`
  - **类型**: `char(1)`
  - **默认值**: `'0'`
  - **注释**: `'删除标志（0代表存在 2代表删除）'`
- **表**: `sys_dict_data`
  - **字段**: `del_flag`
  - **类型**: `char(1)`
  - **默认值**: `'0'`
  - **注释**: `'删除标志（0代表存在 2代表删除）'`

## 2. 验证
再次检查表结构，确认字段添加成功。
我将分步读取所有 Controller 和 DTO 文件，并在根目录生成 `API_DOCS.md` 文件。

文档结构示例：

````markdown
# 认证管理

## 用户登录
- **Path**: `/auth/login`
- **Method**: POST
- **Headers**: 
  - Content-Type: application/json

### 请求参数
| 参数名 | 类型 | 必填 | 描述 | 示例 |
| :--- | :--- | :--- | :--- | :--- |
| user_name | string | 是 | 用户名 | admin |
| password | string | 是 | 密码 | 123456 |

### 响应数据
**类型定义**:
```typescript
interface LoginResponseDto {
  token: string; // 访问令牌
}
````

**示例**:

```json
{
  "code": 200,
  "message": "Login successful",
  "data": {
    "token": "eyJhbG..."
  }
}
```

```

### 执行步骤
1.  **读取 Auth 模块**: 解析 `auth.controller.ts` 及相关 DTO。
2.  **读取 System 模块**: 依次解析用户、角色、菜单、部门、岗位、字典、配置、通知的 Controller 和 DTO。
3.  **读取 Monitor 模块**: 解析日志和监控相关的 Controller 和 DTO。
4.  **生成文档**: 汇总所有解析出的信息，按模块组织 Markdown 内容。
5.  **写入文件**: 将最终内容写入项目根目录的 `API_DOCS.md`。
```

